@page "/markdown"
@inject HttpClient Http

<AcBlog.UI.Components.Markdown.MarkdownRenderer Value="@Value" />

@code{
    private string Value = "";

    protected override async Task OnInitializedAsync()
    {
        Value = await Http.GetStringAsync("data/article.md");
        await base.OnInitializedAsync();
    }
}